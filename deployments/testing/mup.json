{
  // Server authentication info
   "servers": [
    {
      "host": "104.131.50.243",
      "username": "root",
      "password": "varunkthakur"
      // or pem file (ssh based authentication)
      //"pem": "~/.ssh/id_rsa"
    }
  ],

  // Install MongoDB on the server. Does not destroy the local MongoDB on future setups
  "setupMongo": false,

  // Application name (no spaces).
  "appName": "cloudvakiltesting2",

  // Location of app (local directory). This can reference '~' as the users home directory.
  // i.e., "app": "~/Meteor/my-app",
  // This is the same as the line below.
  "app": "../../",

  // Configure environment
  // ROOT_URL must be set to your correct domain (https or http)
  "env": {
    "PORT": 9009,
    "ROOT_URL": "http://lel.tk",
    "MONGO_URL": "mongodb://cloudvakil:devnull@c760.lamppost.3.mongolayer.com:10760,lamppost.10.mongolayer.com:10103/testdb",
    "MONGO_OPLOG_URL": "mongodb://cloudvakil:devnull@c760.lamppost.3.mongolayer.com:10760,lamppost.10.mongolayer.com:10103/local?authSource=testdb"
  },

  // Meteor Up checks if the app comes online just after the deployment.
  // Before mup checks that, it will wait for the number of seconds configured below.
  "deployCheckWaitTime": 60,

  // show a progress bar while uploading.
  // Make it false when you deploy using a CI box.
  "uploadProgressBar": true
}